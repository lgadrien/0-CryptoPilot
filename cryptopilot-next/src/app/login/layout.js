export const metadata = {
  title: "Connexion",
  description: "Connectez-vous à votre tableau de bord CryptoPilot.",
};

export default function LoginLayout({ children }) {
  return <>{children}</>;
}
